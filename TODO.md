# Tasks Complete ✅

**Hero Section**: Spotlight mouse-follow + city.jpeg background.

**DS Project Fixes** (ds.html):
- [x] Gallery grid changed from `1fr` stack to `repeat(12, 1fr)` masonry (like dinning/spotify).
- [x] Tiles classed `.span-7`/`.span-5`/`.span-7` for varied sizing.
- [x] Responsive: Stacks full-width <980px.
- [x] Lightbox zoom/scroll preserved.

Images now properly sized in grid layout, scrollable via lightbox (matches others).

**Demo**: `start ds.html` or via index.html#projects → DS card.

